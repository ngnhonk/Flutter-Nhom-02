specVersion: "v1beta"
serviceId: "taskmanager"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taskmanager-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
